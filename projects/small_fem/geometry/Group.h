#ifndef _GROUP_H_
#define _GROUP_H_

/**
   @interface Group
   @brief A group of elements

   This is the interface allowing @em access to a group
   of elements.
 */

class Group{
 public:
  int getNumElements(void);
  
};

#endif
